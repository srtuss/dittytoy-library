version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
    nodejs: "20"
  jobs:
    post_install:
      - npm install -g jsdoc
      - jsdoc -X src/*

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py