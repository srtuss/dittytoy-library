version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.14"
    nodejs: "24"
  jobs:
    post_install:
      - npm install -g jsdoc

python:
  install:
    - requirements: doc/requirements.txt

sphinx:
  configuration: doc/source/conf.py